{% macro image(
		className = '',
		fileName = 'pay-pal.svg',
		width = '32',
		height = '32',
		alt = 'Alt text'
) -%}

	<img
		class="image {{ className }}"
		src="../../assets/images/{{ fileName }}"
		width="{{ width }}"
		height="{{ height }}"
		alt="{{ alt }}"
	>

{%- endmacro %}
