{% macro image(
	className = '',
	fileName = 'pay-pal.svg',
	height = '32',
	width = '32',
	altText = 'Изображение'
) -%}

	<img
		class="image {{ className }}"
		src="../../assets/images/{{ fileName }}"
		height="{{ height }}"
		width="{{ width }}"
		alt="{{ altText }}"
	>

{%- endmacro %}
