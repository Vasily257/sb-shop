{% macro image(
		className = '',
		fileName = 'pay-pal.svg',
		width = '32',
		height = '32',
		altText = 'Alt text'
) -%}

	<img
		class="image {{ className }}"
		src="../../assets/images/{{ fileName }}"
		width="{{ width }}"
		height="{{ height }}"
		alt="{{ altText }}"
	>

{%- endmacro %}
